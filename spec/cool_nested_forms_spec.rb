require "spec_helper"

describe CoolNestedForms do
  it "has a version number" do
    expect(CoolNestedForms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
